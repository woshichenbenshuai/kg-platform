<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kgplatform.business.kinder.mapper.master.MasterTenantMapper">
    <resultMap id="TenantResultMap" type="com.kgplatform.business.kinder.domain.dto.TenantDto">
        <id column="id" property="id"/>
        <result column="tenant_code" property="tenantCode"/>
        <result column="tenant_name" property="tenantName"/>
        <result column="contact_name" property="contactName"/>
        <result column="contact_phone" property="contactPhone"/>
        <result column="address" property="address"/>
        <result column="expire_date" property="expireDate"/>
    </resultMap>

    <select id="selectEnabledById" resultMap="TenantResultMap">
        SELECT id, tenant_code, tenant_name, contact_name, contact_phone, address, expire_date
        FROM sys_tenant
        WHERE id = #{tenantId}
          AND delete_status = 0
          AND status = 1
        LIMIT 1
    </select>
</mapper>
