<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kgplatform.business.kinder.mapper.master.MasterParentAccountMapper">
    <select id="selectUserIdByUsername" resultType="java.lang.Long">
        SELECT id
        FROM sys_user
        WHERE username = #{username}
          AND delete_status = 0
        LIMIT 1
    </select>

    <insert id="insertUser">
        INSERT INTO sys_user (
          id, username, nickname, phone, password, status, delete_status,
          create_by, create_time, last_modified_by, last_modified_time
        ) VALUES (
          #{id}, #{username}, #{nickname}, #{phone}, #{password}, 1, b'0',
          'system', NOW(), 'system', NOW()
        )
    </insert>

    <select id="selectParentRoleId" resultType="java.lang.Long">
        SELECT id
        FROM sys_role
        WHERE role_code = 'PARENT_PORTAL'
          AND delete_status = 0
          AND status = 1
        LIMIT 1
    </select>

    <select id="selectUserTenantId" resultType="java.lang.Long">
        SELECT id
        FROM sys_user_tenant
        WHERE user_id = #{userId}
          AND tenant_id = #{tenantId}
          AND identity_type = 'PARENT'
          AND delete_status = 0
        LIMIT 1
    </select>

    <insert id="insertUserTenant">
        INSERT INTO sys_user_tenant (
          id, user_id, tenant_id, identity_type, default_flag, status, delete_status,
          create_by, create_time, last_modified_by, last_modified_time
        ) VALUES (
          #{id}, #{userId}, #{tenantId}, 'PARENT', b'1', b'1', b'0',
          'system', NOW(), 'system', NOW()
        )
    </insert>

    <select id="selectUserRoleId" resultType="java.lang.Long">
        SELECT id
        FROM user_role
        WHERE user_id = #{userId}
          AND role_id = #{roleId}
          AND delete_status = 0
        LIMIT 1
    </select>

    <insert id="insertUserRole">
        INSERT INTO user_role (
          id, user_id, role_id, status, delete_status,
          create_by, create_time, last_modified_by, last_modified_time
        ) VALUES (
          #{id}, #{userId}, #{roleId}, b'1', b'0',
          'system', NOW(), 'system', NOW()
        )
    </insert>
</mapper>
