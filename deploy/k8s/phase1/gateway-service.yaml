apiVersion: v1
kind: Service
metadata:
  name: kg-gateway
  namespace: kg-platform
spec:
  selector:
    app: kg-gateway
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
