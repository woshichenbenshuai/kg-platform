<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kgplatform.system.mapper.UserTenantRoleMapper">
    <!--@Table user_tenant_role-->
    <resultMap id="BaseResultMap" type="com.kgplatform.system.domain.dto.UserTenantRoleDto">
        <id column="id" property="id"/>
        <result column="user_tenant_id" property="userTenantId"/>
        <result column="role_id" property="roleId"/>
        <result column="status" property="status"/>
        <result column="delete_status" property="deleteStatus"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_tenant_id, role_id, status, delete_status, create_by, create_time, last_modified_by, last_modified_time
    </sql>

    <select id="selectPageList" resultMap="BaseResultMap">
        SELECT
            <include refid="Base_Column_List"/>
        FROM user_tenant_role
        <where>
            1=1
            AND delete_status = 0
            <if test="null != vo.bindUserTenantId">
                AND user_tenant_id = #{vo.bindUserTenantId}
            </if>
            <if test="null != vo.bindRoleId">
                AND role_id = #{vo.bindRoleId}
            </if>
            <if test="null != vo.status">
                AND status = #{vo.status}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>
</mapper>

