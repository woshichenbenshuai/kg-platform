<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kgplatform.system.mapper.TenantMapper">
    <!--@Table sys_tenant-->
    <resultMap id="BaseResultMap" type="com.kgplatform.system.domain.dto.TenantDto">
        <id column="id" property="id"/>
        <result column="tenant_code" property="tenantCode"/>
        <result column="tenant_name" property="tenantName"/>
        <result column="contact_name" property="contactName"/>
        <result column="contact_phone" property="contactPhone"/>
        <result column="address" property="address"/>
        <result column="expire_date" property="expireDate"/>
        <result column="remarks" property="remarks"/>
        <result column="status" property="status"/>
        <result column="delete_status" property="deleteStatus"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, tenant_code, tenant_name, contact_name, contact_phone, address, expire_date, remarks, status, delete_status, create_by, create_time, last_modified_by, last_modified_time
    </sql>

    <select id="selectPageList" resultMap="BaseResultMap">
        SELECT
            <include refid="Base_Column_List"/>
        FROM sys_tenant
        <where>
            1=1
            AND delete_status = 0
            <if test="null != vo.tenantName and '' != vo.tenantName">
                AND tenant_name LIKE CONCAT('%', #{vo.tenantName}, '%')
            </if>
            <if test="null != vo.tenantCode and '' != vo.tenantCode">
                AND tenant_code = #{vo.tenantCode}
            </if>
            <if test="null != vo.status">
                AND status = #{vo.status}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>
</mapper>

